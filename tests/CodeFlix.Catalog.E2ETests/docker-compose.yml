version: '3.4'

services:
  codeflic.catalog.db:
    container_name: catalogdb-e2e
    hostname: catalogdb-e2e
    image: mysql
    ports:
    - 33060:3306
    environment:
      MYSQL_ROOT_PASSWORD: root
 
